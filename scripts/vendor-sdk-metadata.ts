#!/usr/bin/env tsx
/**
 * vendor-sdk-metadata.ts
 *
 * Reads @weclapp/sdk to extract entity names, field metadata, and enum values,
 * then writes nodes/Weclapp/generated/entityMetadata.ts.
 *
 * Full implementation in unit 23.
 * Run via: npm run codegen
 */

console.log('SDK metadata vendor — implemented in unit 23');
console.log('Skipping: nodes/Weclapp/generated/entityMetadata.ts not updated');
process.exit(0);
