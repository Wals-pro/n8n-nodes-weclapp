#!/usr/bin/env tsx
/**
 * generate-descriptors.ts
 *
 * Reads docs/weclapp-openapi.yaml and nodes/Weclapp/generated/entityMetadata.ts
 * to generate stub descriptor files for each resource under nodes/Weclapp/descriptions/.
 *
 * Full implementation in unit 23.
 * Run via: npm run codegen
 */

console.log('Descriptor codegen — implemented in unit 23');
console.log('Skipping: resource descriptor stubs not regenerated');
process.exit(0);
